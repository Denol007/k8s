name: 📦 Build and Push on Push

# Этот workflow запускается при каждом push в любую ветку
# Он просто собирает и пушит образы в DockerHub

on:
  push:
    branches:
      - '**'  # Любая ветка
    paths:
      - 'services/**'

env:
  DOCKER_USERNAME: denol007

jobs:
  build-and-push:
    name: 🚀 Build & Push Changed Services
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2  # Нужно для сравнения с предыдущим коммитом
      
      - name: Detect changed services
        id: changes
        run: |
          # Определяем какие сервисы изменились
          CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD)
          
          echo "Changed files:"
          echo "$CHANGED_FILES"
          
          USER_CHANGED=false
          PRODUCT_CHANGED=false
          ORDER_CHANGED=false
          PAYMENT_CHANGED=false
          
          if echo "$CHANGED_FILES" | grep -q "services/user-service/"; then
            USER_CHANGED=true
          fi
          
          if echo "$CHANGED_FILES" | grep -q "services/product-service/"; then
            PRODUCT_CHANGED=true
          fi
          
          if echo "$CHANGED_FILES" | grep -q "services/order-service/"; then
            ORDER_CHANGED=true
          fi
          
          if echo "$CHANGED_FILES" | grep -q "services/payment-service/"; then
            PAYMENT_CHANGED=true
          fi
          
          echo "user_changed=$USER_CHANGED" >> $GITHUB_OUTPUT
          echo "product_changed=$PRODUCT_CHANGED" >> $GITHUB_OUTPUT
          echo "order_changed=$ORDER_CHANGED" >> $GITHUB_OUTPUT
          echo "payment_changed=$PAYMENT_CHANGED" >> $GITHUB_OUTPUT
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      # ============================================
      # USER SERVICE
      # ============================================
      - name: 🔨 Build User Service
        if: steps.changes.outputs.user_changed == 'true'
        uses: docker/build-push-action@v5
        with:
          context: services/user-service
          push: true
          tags: |
            ${{ env.DOCKER_USERNAME }}/user-service:latest
            ${{ env.DOCKER_USERNAME }}/user-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: ✅ User Service Built
        if: steps.changes.outputs.user_changed == 'true'
        run: |
          echo "✅ user-service:latest pushed to DockerHub"
          echo "Image: ${{ env.DOCKER_USERNAME }}/user-service:latest"
      
      # ============================================
      # PRODUCT SERVICE
      # ============================================
      - name: 🔨 Build Product Service
        if: steps.changes.outputs.product_changed == 'true'
        uses: docker/build-push-action@v5
        with:
          context: services/product-service
          push: true
          tags: |
            ${{ env.DOCKER_USERNAME }}/product-service:latest
            ${{ env.DOCKER_USERNAME }}/product-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: ✅ Product Service Built
        if: steps.changes.outputs.product_changed == 'true'
        run: |
          echo "✅ product-service:latest pushed to DockerHub"
      
      # ============================================
      # ORDER SERVICE
      # ============================================
      - name: 🔨 Build Order Service
        if: steps.changes.outputs.order_changed == 'true'
        uses: docker/build-push-action@v5
        with:
          context: services/order-service
          push: true
          tags: |
            ${{ env.DOCKER_USERNAME }}/order-service:latest
            ${{ env.DOCKER_USERNAME }}/order-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: ✅ Order Service Built
        if: steps.changes.outputs.order_changed == 'true'
        run: |
          echo "✅ order-service:latest pushed to DockerHub"
      
      # ============================================
      # PAYMENT SERVICE
      # ============================================
      - name: 🔨 Build Payment Service
        if: steps.changes.outputs.payment_changed == 'true'
        uses: docker/build-push-action@v5
        with:
          context: services/payment-service
          push: true
          tags: |
            ${{ env.DOCKER_USERNAME }}/payment-service:latest
            ${{ env.DOCKER_USERNAME }}/payment-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: ✅ Payment Service Built
        if: steps.changes.outputs.payment_changed == 'true'
        run: |
          echo "✅ payment-service:latest pushed to DockerHub"
      
      # ============================================
      # SUMMARY
      # ============================================
      - name: 📊 Deployment Summary
        run: |
          echo "# 🚀 Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Services Built & Pushed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.changes.outputs.user_changed }}" == "true" ]; then
            echo "✅ **user-service** → \`${{ env.DOCKER_USERNAME }}/user-service:latest\`" >> $GITHUB_STEP_SUMMARY
          fi
          
          if [ "${{ steps.changes.outputs.product_changed }}" == "true" ]; then
            echo "✅ **product-service** → \`${{ env.DOCKER_USERNAME }}/product-service:latest\`" >> $GITHUB_STEP_SUMMARY
          fi
          
          if [ "${{ steps.changes.outputs.order_changed }}" == "true" ]; then
            echo "✅ **order-service** → \`${{ env.DOCKER_USERNAME }}/order-service:latest\`" >> $GITHUB_STEP_SUMMARY
          fi
          
          if [ "${{ steps.changes.outputs.payment_changed }}" == "true" ]; then
            echo "✅ **payment-service** → \`${{ env.DOCKER_USERNAME }}/payment-service:latest\`" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Next Steps" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Образы готовы в DockerHub! Теперь можно:" >> $GITHUB_STEP_SUMMARY
          echo "1. Локально: \`kubectl set image deployment/user-service user-service=${{ env.DOCKER_USERNAME }}/user-service:latest -n microservices\`" >> $GITHUB_STEP_SUMMARY
          echo "2. Или перезапустить: \`kubectl rollout restart deployment/user-service -n microservices\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📦 Commit: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "👤 Author: @${{ github.actor }}\`" >> $GITHUB_STEP_SUMMARY
